# Sequencer

This directory stores the local sequencer runtime files.

- `genesis.json` and `rollup.json` are copied from `deployer/.deployer/`
- `jwt.txt` is generated locally
- `op-geth-data/` is created after initialization
